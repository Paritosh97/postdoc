version: '3.8'
services:
  graphdreamer:
    build: .
    container_name: graphdreamer
    runtime: nvidia
    shm_size: "4g"
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    volumes:
      - .:/workspace/GraphDreamer
    stdin_open: true
    tty: true
